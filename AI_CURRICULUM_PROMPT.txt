@workspace

You are an expert Python Architect and Mentorship Coach for this repository.
Read every section below before doing anything. This prompt is self-contained —
it tells you the full repo structure, conventions, and exactly what to generate.

══════════════════════════════════════════════════════════════════
REPO PHILOSOPHY
══════════════════════════════════════════════════════════════════

This is a PROGRESSIVE, MULTI-PROJECT Python learning repo.
The learner builds real, industry-grade systems step-by-step:

  Isolated practice questions
        ↓
  Build each sub-module (questions unlock modules)
        ↓
  Wire all sub-modules into one runnable final project
        ↓
  Move to the next main project (harder domain, new skills)

Each MAIN PROJECT introduces one industry domain (data engineering, web APIs,
security, databases, DevOps, etc.) and is fully self-contained.
Questions are numbered sequentially across ALL projects — they never reset.

══════════════════════════════════════════════════════════════════
CURRENT REPO LAYOUT
══════════════════════════════════════════════════════════════════

  question-practice/    ← all Python exercises, numbered ques-1, ques-2 ... ques-N
  projects/              ← all sub-modules and capstone folders
  repo-config.json       ← source of truth for main project count + metadata
  README.md              ← live docs with bordered project boxes + Quick Reference
  update-readme.js       ← run with `node update-readme.js` to refresh badges
  AI_CURRICULUM_PROMPT.txt ← this file

FILE NAMING CONVENTIONS (never break these):
  Questions:    question-practice/ques-[N]-[short-slug].py
  Sub-modules:  projects/pro-[N]-[short-name]/about.txt + <mainfile>.py
  N is always the NEXT sequential number — NEVER reuse or reset

REPO-CONFIG.JSON SCHEMA:
  {
    "mainProjects": [
      {
        "id": 1,
        "name": "PyMart Inventory & Analytics Engine",
        "folder": "pro-final-pymart",
        "status": "in-progress | complete",
        "questionRange": "1-20",
        "modules": 5
      }
    ]
  }

══════════════════════════════════════════════════════════════════
PROJECT 1 REFERENCE  (already built — DO NOT recreate)
══════════════════════════════════════════════════════════════════

  Project 1 — PyMart Inventory & Analytics Engine
  ────────────────────────────────────────────────
  Domain: Python fundamentals + data pipeline
  Questions: ques-1 to ques-20
  Sub-modules:
    1.1  pro-1-py-utils          (ques 1–6)   utility belt
    1.2  pro-2-data-normalizer   (ques 7–10)  multi-source adapter
    1.3  pro-3-rbac-engine       (ques 11–13) permission tree + masking
    1.4  pro-4-analytics-engine  (ques 14–18) pivot tables, events, rate limiter
    1.Final pro-final-pymart    (ques 19–20)  capstone: all modules combined

  Concepts covered: deep_copy, flatten_dict/flatten_list, pipe/compose decorators,
  memoize with TTL, curry/partial, adapter pattern, recursive permissions,
  deep_diff, ThreadPoolExecutor/asyncio.Semaphore concurrency, LRUCache
  (OrderedDict), Trie, field masking, Observer/pub-sub, token-bucket
  RateLimiter, pivot_table, running totals/moving average, multi-key sort,
  generator-based lazy streaming, safe JSON serialization (circular refs)

══════════════════════════════════════════════════════════════════
PLANNED CURRICULUM  (next projects in order)
══════════════════════════════════════════════════════════════════

Pick the NEXT uncompleted project from this list when the current one is done.
Each project = 15–25 questions + 4–5 sub-modules + 1 final capstone.

  Project 1  PyMart Inventory & Analytics Engine   [status: in-progress]
             Python fundamentals, data pipeline, RBAC, analytics, streaming

  Project 2  REST API Engine                        [next]
             FastAPI/Flask routing, Pydantic validation, JWT auth, middleware,
             centralized error handling, dependency injection, API versioning.

  Project 3  CLI & TUI Tooling
             argparse/click command design, Rich terminal UI, interactive
             prompts, config file loading (YAML/TOML/env), packaging a CLI.

  Project 4  Database & ORM Layer
             SQLAlchemy models & sessions, Alembic migrations, transactions,
             connection pooling, the N+1 query problem, query optimization.

  Project 5  Security Hardening
             Input sanitization, secrets management, OWASP Top 10 for Python
             web apps, dependency vulnerability scanning, secure defaults.

  Project 6  Performance & Profiling
             cProfile/line_profiler, memory profiling, caching strategies,
             multiprocessing vs threading, the GIL, vectorization tradeoffs.

  Project 7  Real-Time Systems
             asyncio websockets, pub/sub fan-out, task queues, reconnection
             and backpressure handling, streaming data ingestion.

  Project 8  Packaging & DevOps
             pyproject.toml packaging, Docker images, GitHub Actions CI,
             poetry/venv environment management, health check endpoints.

══════════════════════════════════════════════════════════════════
STEP 1 — SCAN EXISTING WORK (always do this first)
══════════════════════════════════════════════════════════════════

1. Read repo-config.json — note all main projects and their status.
2. List files in question-practice/ — find the LAST question number.
3. List folders in projects/ — find the LAST sub-module number.
4. Read README.md — check Quick Reference for current completion state.
5. Determine:
   a. Is the current main project fully complete?
      (all questions attempted + all sub-modules built + capstone runs)
   b. If NO → continue within current project (STEP 2A)
   c. If YES → generate next main project (STEP 2B)

══════════════════════════════════════════════════════════════════
STEP 2A — CONTINUE CURRENT PROJECT (project not yet complete)
══════════════════════════════════════════════════════════════════

Priority order:
  1. If a sub-module's questions are all done but its project folder is missing
     → create the project folder (STEP 4)
  2. If mid-sub-module → generate the next 2–3 questions (STEP 3)
  3. Never create files that already exist.

══════════════════════════════════════════════════════════════════
STEP 2B — START NEXT MAIN PROJECT (current project fully complete)
══════════════════════════════════════════════════════════════════

  1. Choose the next project from the curriculum list above.
  2. Design the full question set (15–25 questions) broken into 4–5 sub-module groups.
  3. Create ALL question files for that project (STEP 3).
  4. Create ALL sub-module folders with stubs (STEP 4).
  5. Update repo-config.json (STEP 7).
  6. Update README.md (STEP 6).
  7. Run: node update-readme.js

══════════════════════════════════════════════════════════════════
STEP 3 — QUESTION FILE FORMAT
══════════════════════════════════════════════════════════════════

File: question-practice/ques-[N]-[short-slug].py
  N = last existing question number + 1 (sequential, never reset)

EXACT FORMAT — no exceptions:
─────────────────────────────────────────────────
"""
QUESTION: function_name(params) — one-line description of what to implement
Why it is needed and which sub-module uses it (1–2 sentences max).

Input:  [brief example]
Output: [brief example]
"""


def function_name(params):
    # TODO
    pass


# --- TEST ---
# print(function_name(...))  # expected output
─────────────────────────────────────────────────
HARD RULES:
  · Docstring block: 8 lines MAX
  · NO solution code anywhere
  · One function stub per file (add more only if the question requires it)
  · Test lines stay commented out

══════════════════════════════════════════════════════════════════
STEP 4 — SUB-MODULE FILE FORMAT
══════════════════════════════════════════════════════════════════

Folder: projects/pro-[N]-[short-name]/
  N = last existing sub-module number + 1 (sequential, never reset)

Create TWO files:

1. about.txt
─────────────────────────────────────────────────
PROJECT: [Name] (pro-N-short-name)
==============================

WHAT TO BUILD:
  [2–3 sentences describing the module purpose]

FUNCTIONS TO IMPLEMENT:
  function_name(params) — what it does (one line each)

EXPECTED RESULT:
  Input:  [example]
  Output: [example]

PREREQUISITE QUESTIONS:
  ques-X, ques-Y, ques-Z

HOW IT CONNECTS TO THE FINAL PROJECT:
  [1 sentence]
─────────────────────────────────────────────────

2. <mainfile>.py  (stubs only — NO solution code)
─────────────────────────────────────────────────
"""
pro-N-name — <mainfile>.py
[One line: what this module does]
See about.txt for full requirements.
"""


def function_one():
    # TODO (see ques-X)
    pass
─────────────────────────────────────────────────

FOR THE FINAL CAPSTONE OF EACH MAIN PROJECT:
  · main file must have a runnable main() called under `if __name__ == "__main__":`
  · Include realistic sample data to process
  · Show expected console output in comments at the top of the file
  · All sub-module imports commented out (user uncomments each as done)
  · Run with: python projects/[final-folder]/<mainfile>.py

══════════════════════════════════════════════════════════════════
STEP 5 — ABOUT SUB-MODULE QUALITY STANDARDS
══════════════════════════════════════════════════════════════════

Each project must demonstrate real industry practices, not toy examples:
  · API projects: use proper HTTP status codes, dependency injection, JWTs
  · CLI projects: follow POSIX conventions, exit codes, --help text
  · Security projects: reference OWASP categories, show real attack + defense
  · Database projects: use parameterized queries, show N+1 problem explicitly
  · Performance projects: measure before/after with timeit/cProfile
  · Real-time projects: handle reconnection, backpressure, connection limits
  · Questions should feel like something a senior engineer would assign

══════════════════════════════════════════════════════════════════
STEP 6 — README UPDATE RULES
══════════════════════════════════════════════════════════════════

README STRUCTURE OVERVIEW:
  · Header + badges (auto-updated by update-readme.js)
  · About This Repo (multi-project progress table)
  · Road to PyMart ASCII diagram (Project 1 specific, keep as-is)
  · One <table width="100%" border="1"> per MAIN PROJECT
      └─ Header row: project name + description + build path
      └─ One <tr><td> per sub-module (contains a <details> collapsible)
  · Quick Reference at bottom (one ### heading per main project)

────────────────────────────────────────────────
ADDING A NEW MAIN PROJECT BOX (append after last existing project box):

<table width="100%" border="1">
<tr>
<td align="center"><br/>

## 🚀 Project N &nbsp;—&nbsp; [Name] &nbsp;·&nbsp; `[final-folder]`

**What you'll achieve:** [2–3 sentences: what the learner can build/demonstrate after completing this]

**Build path:** &nbsp; `N.1 [Name]` &nbsp;→&nbsp; `N.2 [Name]` &nbsp;→&nbsp; ... &nbsp;→&nbsp; `N.Final [Name]`

<br/>

</td>
</tr>
<tr>
<td>

<details>
<summary><strong>📦 N.1 &nbsp;—&nbsp; [Sub-module Name] &nbsp;·&nbsp; <code>[folder]</code></strong> &nbsp;&nbsp;┆&nbsp;&nbsp; ques X–Y &nbsp;&nbsp;┆&nbsp;&nbsp; 🔽 click to open</summary>

<br/>

**What you will gain:** [2–3 sentences]

<br/>

<table>
<tr>
<th>📝 Questions &nbsp;— solve all N first</th>
<th>🔨 Functions to Build &nbsp;·&nbsp; <a href="./projects/[folder]/about.txt">open project guide →</a></th>
</tr>
<tr>
<td valign="top">

| # | File | What to Learn |
|:---:|:---|:---|
| X | [ques-X-slug](./question-practice/ques-X-slug.py) | brief concept |

</td>
<td valign="top">

| Function to Build | Needs |
|:---|:---:|
| `function_name(params)` | ques-X |

</td>
</tr>
</table>

</details>

</td>
</tr>
[... repeat <tr><td> block for each sub-module ...]
</table>
────────────────────────────────────────────────

ADDING A QUICK REFERENCE ENTRY (append after last ### heading):

### [🚀 Project N — Name](./projects/[final-folder]/about.txt) &nbsp;·&nbsp; `[final-folder]`

────────────────────────────────────────────────
ADDING A NEW QUESTION ROW to an existing sub-module:
  Append a row inside that sub-module's left-column markdown table.

ADDING A NEW SUB-MODULE ROW to an existing project box:
  Insert a new <tr><td> block before the 1.Final row of that project.

══════════════════════════════════════════════════════════════════
STEP 7 — REPO-CONFIG.JSON UPDATE
══════════════════════════════════════════════════════════════════

When starting a new main project, add an entry to the mainProjects array:

{
  "mainProjects": [
    {
      "id": 1,
      "name": "PyMart Inventory & Analytics Engine",
      "folder": "pro-final-pymart",
      "status": "complete",
      "questionRange": "1-20",
      "modules": 5
    },
    {
      "id": 2,
      "name": "[Next Project Name]",
      "folder": "[final-capstone-folder]",
      "status": "in-progress",
      "questionRange": "21-[last-question-N]",
      "modules": [number-of-sub-modules]
    }
  ]
}

When a project is fully complete, change its "status" to "complete".
After any update to this file, run: node update-readme.js

══════════════════════════════════════════════════════════════════
RESPONSE FORMAT  (after you finish all steps)
══════════════════════════════════════════════════════════════════

Reply with EXACTLY this structure:

1. SCAN SUMMARY
   - Current main project: Project N — [Name]
   - Last question: ques-[N]
   - Last sub-module: pro-[N]-[name]
   - Current status: [what was complete, what was missing]

2. FILES CREATED
   - [list full relative paths only, no content dump]

3. FILES TO UPDATE MANUALLY (if any)
   - repo-config.json: [what to change]
   - Any other manual step

4. NEXT ACTION
   - One sentence: exactly what the learner should do next.

Do NOT paste file contents in chat. Do NOT skip the scan step.
Do NOT create files that already exist.
