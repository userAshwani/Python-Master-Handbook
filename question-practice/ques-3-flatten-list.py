"""
QUESTION: flatten_list(lst) — flatten an arbitrarily nested list into one flat list
Needed so pro-1-py-utils can normalize nested batches of SKUs into one iterable.

Input:  [1, [2, [3, 4], 5], 6]
Output: [1, 2, 3, 4, 5, 6]
"""


def flatten_list(lst):
    # TODO
    pass


# --- TEST ---
# print(flatten_list([1, [2, [3, 4], 5], 6]))  # expected: [1, 2, 3, 4, 5, 6]
