"""
Q9: tower_of_hanoi(n, source, target, auxiliary) — recursively solve the
Tower of Hanoi puzzle, returning a list of (from_peg, to_peg) moves needed
to move n disks from `source` to `target`.

Input:  tower_of_hanoi(2, "A", "C", "B")
Output: [("A","B"), ("A","C"), ("B","C")]
"""


def tower_of_hanoi(n, source, target, auxiliary):
    # TODO
    pass


# --- TEST ---
# print(tower_of_hanoi(2, "A", "C", "B"))
# expected: [('A', 'B'), ('A', 'C'), ('B', 'C')]
