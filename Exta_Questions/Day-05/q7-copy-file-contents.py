"""
Q7: copy_file(src_path, dest_path) — copy the full contents of one text
file to another using two `with open(...)` blocks (no shutil).

Input:  copy_file("a.txt", "b.txt")
Output: b.txt now contains exactly what a.txt contained
"""


def copy_file(src_path, dest_path):
    # TODO
    pass


# --- TEST ---
# copy_file("a.txt", "b.txt")
# then compare contents of a.txt and b.txt: expected identical
