"""
Q6: two_sum_indices(numbers, target) — return the indices of the two numbers
that add up to `target`, using a single pass with a dict lookup (not brute force).

Input:  two_sum_indices([2, 7, 11, 15], 9)
Output: (0, 1)
"""


def two_sum_indices(numbers, target):
    # TODO
    pass


# --- TEST ---
# print(two_sum_indices([2, 7, 11, 15], 9))  # expected: (0, 1)
