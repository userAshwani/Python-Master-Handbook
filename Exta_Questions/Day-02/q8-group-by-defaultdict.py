"""
Q8: group_by_length(words) — group a list of words by their length, using
collections.defaultdict(list).

Input:  ["hi", "cat", "at", "dog", "a"]
Output: {2: ["hi", "at"], 3: ["cat", "dog"], 1: ["a"]}
"""


def group_by_length(words):
    # TODO
    pass


# --- TEST ---
# print(group_by_length(["hi", "cat", "at", "dog", "a"]))
# expected: {2: ['hi', 'at'], 3: ['cat', 'dog'], 1: ['a']}
